export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;
  let result;
  if (trueOrFalse) {
    const task = true;
    const task2 = false;
    result[task, task2]
  }

  return [task, task2];
}

